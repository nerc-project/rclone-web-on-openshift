apiVersion: v1
kind: Service
metadata:
  labels:
    app: rclone
    app.kubernetes.io/part-of: rclone-app
  name: rclone
spec:
  ports:
    - name: 5572-tcp
      protocol: TCP
      port: 5572
      targetPort: 5572
  selector:
    app: rclone
  sessionAffinity: None
